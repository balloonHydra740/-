# 复制本文件为 keystore.properties 并填入自己的值。
# keystore.properties 与 *.jks 均已在 .gitignore 中，不会被提交。

# keystore 文件路径（相对于项目根目录，也可写绝对路径）
storeFile=my-release-key.jks
# keystore 密码
storePassword=
# 密钥别名
keyAlias=
# 密钥密码
keyPassword=
